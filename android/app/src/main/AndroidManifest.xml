<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Saving chat images via gal (scoped storage handles API 30+); legacy write for older devices. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- open_filex merges broad media read permissions; we only open PDFs from app cache
         (FileProvider) and pick attachments via the system picker (no persistent gallery access). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />

    <application
        android:label="Wayo Ads"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom-scheme OAuth redirect. No autoVerify: App Links
                 verification only applies to https + Digital Asset Links, so
                 autoVerify on a custom scheme is a no-op (and misleading). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.wayo.wayoadsgo"
                    android:host="oauthredirect" />
            </intent-filter>

            <!-- YouTube OAuth return (FlutterWebAuth2 / HTTPS mobile-callback deep link). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.wayo.wayoadsgo"
                    android:host="youtube-oauth" />
            </intent-filter>

            <!-- Wayo Ads home-widget / product deep links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="wayoads" />
            </intent-filter>

            <!-- Share image/file into Wayo chat (multipart upload, not URL text). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FCM: use same high-importance channel as [wayo_push_service] / flutter_local_notifications. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="wayo_push_high" />

        <!-- Required by Stripe PaymentSheet / PlatformPay for Google Pay. -->
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <!-- Do not tools:replace Stripe activity themes. Overriding Financial
             Connections / Link with Theme.AppCompat crashed ACH prepaid
             ("Wayo Ads keeps stopping"). Nav-bar insets are handled via
             MainActivity setDecorFitsSystemWindows around presentPaymentSheet. -->

        <activity
            android:name="com.linusu.flutter_secure_storage.FlutterSecureStorageActivity"
            android:exported="false" />

        <!-- Required by flutter_local_notifications for tray actions where [showsUserInterface] is false
             (MessagingStyle « Répondre » without launching MainActivity). Without this receiver,
             the broadcast is never delivered: inline reply hangs on the spinner. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!-- Home Screen Widgets (fully-qualified names help Samsung/OEM widget hosts) -->
        <receiver
            android:name="ma.wayo.wayoadsgo.widgets.PerformanceWidgetProvider"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_performance_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_performance_info" />
        </receiver>
        <receiver
            android:name="ma.wayo.wayoadsgo.widgets.WalletWidgetProvider"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_wallet_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_wallet_info" />
        </receiver>
        <receiver
            android:name="ma.wayo.wayoadsgo.widgets.QuickActionsWidgetProvider"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_quick_actions_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_actions_info" />
        </receiver>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- mailto: links (maintenance support email, etc.) -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<!--
  Transport security policy.
  - All production / remote traffic MUST be HTTPS (cleartext blocked everywhere).
  - Local development backends (emulator loopback / localhost) are the ONLY
    hosts allowed over cleartext HTTP, and they are unreachable in production.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
